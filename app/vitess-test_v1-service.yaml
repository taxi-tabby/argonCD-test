apiVersion: v1
kind: Service
metadata:
  name: vitess-test
  namespace: default-test
  labels:
    app: vitess-test
spec:
  selector:
    app: vitess-test
  ports:
  - name: web
    port: 15991
    targetPort: web
  - name: grpc
    port: 15999
    targetPort: grpc
  type: ClusterIP